// States
enum State { idle, running, sprinting, attacking, defending, length }
// Targets
enum Target { noone, obj_player, obj_hub, obj_wall, length }

// Weapons & Items
//enum ammoType { noone, arrows_common, arrows_rare, arrows_legendary}
enum Item { none, walls, sword, bow, inv1, inv2, inv3, length }

enum ItemProperties { sprite, name, amount, maxAmount, length }






